/**
 * Indicates a loop is about to begin
 */
const BEGIN_LOOP = '[';

/**
 * Indicates a loop has ended
 */
const END_LOOP = ']';

/**
 * Indicates the end of a line
 */
const END_OF_LINE = ';';

module.exports = {
  BEGIN_LOOP,
  END_LOOP,
  END_OF_LINE
};
